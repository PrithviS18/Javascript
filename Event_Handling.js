// Events allow you to write Javascript code that reacts to
// certain situationsT

// There are two types of Event Handling
// 1. Event handlers (Used directly in the HTML)
// 2. Event listners (Used in the Javascript using get elements

// Bubbling and capture
// In Bubbling the inner most element is handled First
// But in Capture the outer most element is handled First (by giving true value to listner('click', function(), true))
